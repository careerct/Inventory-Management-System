import {useEffect,useState} from "react";import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer} from "recharts";import API from "../services/api";import Loading from "../components/Loading";
const money=n=>`₹${Number(n||0).toLocaleString("en-IN",{maximumFractionDigits:2})}`;
export default function Reports(){const[d,setD]=useState(null),[error,setError]=useState("");useEffect(()=>{API.get("/reports").then(r=>setD(r.data)).catch(e=>setError(e.response?.data?.message||"Unable to load reports"))},[]);if(error)return <div className="alert">{error}</div>;if(!d)return <Loading/>;return <><div className="page-header"><h2>Reports</h2><p>Business summaries based on recorded transactions.</p></div><div className="stats-grid"><div className="stat-card"><div className="stat-card-title">Sales</div><div className="stat-card-value">{money(d.totalSales)}</div><div className="stat-card-description">{d.salesCount} invoices</div></div><div className="stat-card"><div className="stat-card-title">Purchases</div><div className="stat-card-value">{money(d.totalPurchases)}</div><div className="stat-card-description">{d.purchaseCount} invoices</div></div><div className="stat-card"><div className="stat-card-title">Gross Profit</div><div className="stat-card-value">{money(d.grossProfit)}</div><div className="stat-card-description">Sales margin using stored cost prices</div></div></div><div className="dashboard-card"><div className="dashboard-card-header"><h3>Top Products by Quantity Sold</h3><p>Based on all recorded sales.</p></div><div className="dashboard-card-body">{d.topProducts.length?<ResponsiveContainer width="100%" height={340}><BarChart data={d.topProducts}><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="quantity" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer>:<div className="empty-state">No sales data available.</div>}</div></div></>}
